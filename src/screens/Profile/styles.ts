import { withStyles } from '@utils/hooks';

export const useStyles = withStyles(() => ({
  menuItem: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
}));
